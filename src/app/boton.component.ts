import { Component, Input } from '@angular/core';

@Component({
    selector: 'jl-boton',
    templateUrl: 'boton.component.html'
})
export class BotonComponent {
    texto = 'Botón con texto por defecto';
}
